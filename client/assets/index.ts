// Asset imports — Vite processes these through the build pipeline
// (hashed filenames, optimized, cache busting)
import heroIllustration from "./images/hero-illustration.png";
import loaderGif from "./gifs/loader.gif";
import logo from "./images/logo-foreground.png";

export const images = {
    heroIllustration,
    logo,
};

export const gifs = {
    loader: loaderGif,
};
